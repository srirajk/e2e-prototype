package org.example;

import org.example.common.model.FileRequestLineEvent;

public class MatchFilter {

/*    private FileRequestLineEvent executeFilterChain(final FileRequestLineEvent fileRequestLineEvent){

        FileRequestLineEvent output = fileRequestLineEvent.clone();
        List<MatchModel> matchModels = new ArrayList<>();
        fileRequestLineEvent.getHits().stream().forEach(
                matchModel -> {
                    matchmodel.clone();
                    matchmodel.setDecisionrecord("risk");
                }
        );
        });
        
        
    }*/

}
